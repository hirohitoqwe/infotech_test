## Тестовое задание компании Инфотех

### Build:
- Config .env file
```bash
    composer update
    php artisan key:generate
    php artisan migrate
    npm i
    npm run dev
    php artisan serve
```
